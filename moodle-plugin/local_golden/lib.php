<?php
// Navigation hook for local_golden.
// The "Site admin → Reports → GOLDEN Map" link is registered in settings.php via
// $ADMIN->add('reports', new admin_externalpage(...)). No additional navigation hooks
// are required.

defined('MOODLE_INTERNAL') || die();
